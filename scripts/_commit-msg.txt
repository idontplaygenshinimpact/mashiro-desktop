fix: 追问判定改质量驱动 + 复盘报告可读性（Markdown 渲染 + 对比度）

【追问判定：不做机械硬限制，按回答质量判断】
- prompt 重写判定原则：回答完整/准确/有深度 → 切题（new/stage）；
  有明显漏洞/含糊 → followup 继续深挖；跑题/不会 → 换题不硬追
- MAX_DEPTH 3→6：仅作防死循环安全阀（正常按质量判断不会走到），
  不再 3 次机械切题

【复盘报告看不清字】
- 根因：面板 body 背景图 + 毛玻璃卡片（rgba 白 0.8 半透明）+ 无显式文字色
  → 背景图透出，部分文字对比度不足
- 修复：iv-review 背景实化 0.96 + 显式深色文字 #2c2a4a
- 升级：复盘报告改用 renderMd Markdown 渲染（标题/列表/加粗/表格/代码块
  层次清晰；renderMd 全转义 + href 净化，LLM 输出安全），补 sd-* 样式

测试：643/643 全绿。
