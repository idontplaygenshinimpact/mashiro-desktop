fix: 语音输入打通全链路——三层根因（无声默认设备 / ScriptProcessor 全零 / 返回解包 bug）

日志驱动的三层修复：
1. 采集 API：ScriptProcessor 在 Electron 43 下 inputBuffer 全零（实测 0/106496 非零）
   → 换 AudioWorklet（pcm-worklet.js），面板 + 面试录音同步替换
2. 设备选择：Windows 默认输入设备是无声的 MCHOSE 麦克风（探针实测默认=0 非零，
   显式 Realtek=98.5% 非零）→ enumerateDevices + 优先物理麦克风（Realtek/麦克风）
3. 返回解包：worker 化时 handler 把整个响应对象当文本返回（text.slice 崩溃，
   r.text 一直是对象）→ 解包 result.text 为字符串
附：主进程日志持久化（追加模式，relaunch 不丢）、面板内语音提示行、VAD/能量诊断日志、
采集自检 2s、处理链禁用（防设备组合下全零）

验证：639/639 全绿；真实链路：探针 98.5% 非零 → VAD 放行 → sherpa 识别。
