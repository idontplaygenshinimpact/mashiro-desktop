test: 模拟面试问题显示链路 jsdom 回归——「面试看不到问题」根因验证

链路复盘：开始面试 → startIvSession → $("iv-answer-area").style.display = ""
（iv-answer-area 是 class 被当 id 用 → 此处必抛 TypeError）→ showQuestion 永不执行
→ 问题不显示 + 界面卡「面试官准备中」。上一提交已补 id，本提交补交互测试闭环验证：
点击「🎤 开始模拟面试」→ 断言问题文本显示 / 回答区可见 / 状态切「面试中」。
修复前该用例必挂，修复后 4/4 通过。全量 616/616。
