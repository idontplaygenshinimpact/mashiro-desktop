# 复习卡去重与口径统一工单（4 个任务）

> 背景：复习卡漂移分裂（状态机 12 张）、薄弱点/清单阈值口径不清、复习卡讲解方向偏岗位、伪知识点过滤两套实现。

## 任务与实施

### 任务 1：复习卡去重归一化

```
问题：状态机 12 张漂移卡（"状态机与异步并发/异步状态机与并发提交控制/状态机状态定义与重复提交拦截机制"同一知识点多卡）
改动：
- addCard 先精确后相似（isSimilarTopicForArchive 2-gram 门槛——比 isSimilarWeakTopic 严，
  防误并"状态机与SSE数据流联动"和"状态机与接口联动"）——相似合并到已有卡，不新建
- 合并语义：精确匹配后建覆盖（现有行为）；相似合并 question/answer 取更完整
  （防薄弱点短 question 覆盖手写题库详细 question）；source 保留原卡（先建来源更具体）
- mergeSimilarCards()：存量漂移卡归一化（幂等）——同簇保留一张，手动级联删 card_reviews
  （SQLite 外键默认关闭，DELETE 有复习记录的卡会 FOREIGN KEY constraint failed）
- 真实库验证：220 → 201 张（merged 19），状态机簇 12 → 9（剩余为不同知识点：竞态/异常/SSE/接口）
```

### 任务 2：阈值统一（写清决策）

```
现状：addFailedToPlan total<50 进清单；flowWeakPoint total<60 进薄弱点——50-59 只进薄弱点不进清单
决策：保持分层（不改逻辑，注释写清口径）——
  - <60 → 薄弱点（部分掌握也回流，靠复习卡补）
  - <50 → 薄弱点 + 学习清单（明确答错，强信号直接进清单）
  - 50-59（partial）只进薄弱点：一次"半会不会"就进清单会堆满低质量条目；
    反复 partial 经薄弱点 fail_count≥2 自动入清单（薄弱点闭环工单）——分层闭环完整
  - 与 learning_events 口径一致：>=60 pass / >=50 partial / <50 fail
```

### 任务 3：复习卡讲解方向统一

```
routes/review.mjs:164 positionDefault → "面试"：复习错题讲解不按岗位方向（卡是跨岗位知识点，
用 positionDefault 会让讲解偏岗位）；grep 清残留——其余 positionDefault 保留（模拟面试岗位默认值）
```

### 任务 4：伪知识点过滤统一

```
cleanWeakTopic 委托 memory._cleanTopic（单一实现）：此前两套独立模式（PSEUDO_PATTERNS 40 字
vs _cleanTopic 30 字+更全模式）会漂移（"题1【…】"题目占位符/测试残留"到期新卡"只在 _cleanTopic 拦）
```

## 验收

- [x] 状态机漂移卡合并（真实库 12→9，幂等）
- [x] 阈值分层决策写清（注释）
- [x] 复习卡讲解方向"面试"（无 positionDefault 残留）
- [x] cleanWeakTopic 单一实现（委托 _cleanTopic）
- [x] npm test 全绿 + lint 0 + typecheck 0
