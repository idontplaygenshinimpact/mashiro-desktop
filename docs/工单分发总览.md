# 工单分发总览（评估报告 2026-09 + TS 升级探索）

> 来源：`docs/项目全面评估报告-2026-09.md`（8 轮审计，9 严重 + 15 中等 + 16 轻微）+ 通用 agent 语言探索（TS 升级决策）。共 **7 个工单**。
> **2026-09 验收结论：评估报告已严重过期——9 严重项中 8 项已修复（S1-S8），工单需以本总览"验收结果"为准，勿重复修已修复项。**

---

## 工单清单

| # | 工单 | 覆盖问题 | 工作量 | 优先级 | 状态 |
|---|---|---|---|---|---|
| 1 | `docs/安全加固工单.md` | S1-S4, S9, M2-M6, M14, L6, L10 | ~2 天 | 🔴 最高 | ✅ **全部完成**（含 L6 测试、L10 复用） |
| 2 | `docs/数据完整性工单.md` | S5-S7, M11, M12 | ~1 天 | 🔴 高 | ✅ **全部完成，关闭** |
| 3 | `docs/测试与CI工单.md` | M1, M8, L5 + 沙箱测试缺口 | ~1 天 | 🟡 中 | ✅ **全部完成**（沙箱 ③c/③d、security-modules.test.mjs、临时目录） |
| 4 | `docs/发布与文档工单.md` | S8, M13, L5, L8, L11, L14, L16 | ~1.5 天 | 🟡 中 | ✅ **全部完成**（mcp.md 已改 13 个） |
| 5 | `docs/性能工单.md` | M7, M9, M10 + cc-watcher/rag/memory | ~2 天 | 🟢 低 | ✅ **基本完成**（剩 memory mergeSimilarWeakPoints 全表，低优先） |
| 6 | `docs/技术债清理工单.md` | L1-L4, L7, L9, L12, L13, L15 | ~1.5 天 | 🟢 低 | ✅ **基本完成**（extractJson 已收敛 llm.mjs 单点） |
| 7 | `docs/TS升级工单.md` | 渐进式 TS 升级 | 任务1 ~0.5 天 | 🟢 低 | ⚠️ **任务 1 已做**（checkJs+JSDoc+全绿），剩 memory/tools JSDoc 缺口；任务 2/3 未做 |

## 验收结果（2026-09，逐项核对代码）

### ✅ 已修复（评估报告过期项，勿重复修）

| 项 | 问题 | 验证证据 |
|---|---|---|
| S1 | .env.bak 明文 key | 文件已删除（glob 0 命中） |
| S2 | 判题沙箱 getBuiltinModule 逃逸 | `sandbox-worker.mjs:16` 遮蔽列表已含 `getBuiltinModule`/`_linkedBinding`（注释"2026-09 再修（安全工单 S2）"） |
| S3 | 判题断言可伪造 | `sandbox-worker.mjs:61-70` 断言改 `writable:false + configurable:false`（注释"2026-09 再修（安全工单 S3）"） |
| S4 | 无 setWindowOpenHandler + IPC 无 sender 校验 | `main.mjs:198-221` safeHandle 包装器 + KNOWN_WINDOWS 校验；`:208,213` setWindowOpenHandler + will-navigate |
| S5 | 复盘判分失败仍标记已复盘 | `study-review.mjs:97-99` 解析失败返回 `{ok:false}` 不标记（注释"修复 S5"） |
| S6 | addCard 写库失败静默吞 | `review.mjs:129-132` console.warn + 返回 `{ok:false}`（注释"修复 S6"） |
| S7 | savePlan 写库失败静默吞 | `study-store.mjs:64-68` console.error + return false（注释"修复 S7"） |
| S8 | 发布包 files 缺 dev-history-guide | `package.json:22` files 已含整个 `"skills/"` 目录 |
| M2 | MCP 自环 permission auto | `data/mcp-servers.json:8` 已改 `"confirm"` |
| M3 | toolFetchPage 重定向未复检 | `impl-search.mjs:154-158` 重定向后复检 assertPublicUrl（注释"安全工单 M3"） |
| M4 | Vue 窗口无 CSP | `desktop/renderer/panel-vue-review/dist/index.html:4` 已有完整 CSP meta |
| M5 | _toastFallback 死代码 | 已删除（grep 0 命中） |
| M11 | indexPersonalProjects 无事务 | `personal-projects.mjs:129` 已包 withTx |
| M12 | UTC/本地时区混用 10+ 处 | 全库仅剩 2 处 `toISOString().slice(0,10)`（output-import.mjs:28 有注释说明是有意同构） |
| M14 | skill 工具默认 auto | `skills.mjs:128-129` 已改"只有显式 auto 才自动执行"（默认 confirm） |

### ⚠️ 仍存在（工单有效，需执行）

| 项 | 问题 | 验证证据 |
|---|---|---|
| M9 | 渲染层 6 个无条件轮询 | `panel-rest.js:1261,1431,1445,1447,1449,1451` 全部仍在 |
| M10 | React 版 ScriptProcessor 采集麦克风 | `desktop/renderer/panel-react/src/panel.jsx:210` 仍 `createScriptProcessor` |
| M13 | mcp.md 工具数自相矛盾 | `docs/mcp.md:8` 说 13 个，`:85,141` 说 9 个 |
| L1 | extractJson 重复定义 | 仍 4 处本地定义（ai.mjs:8 / dreaming.mjs:14 / mail.mjs:298 / rss.mjs:85）+ llm.mjs:374 公共导出 |
| — | cc-watcher 每 2s 全量读文件 | `cc-watcher.mjs:81` 仍 readFileSync 全量 |
| — | rag 全表加载 / memory 全表扫描 | 未验证（性能工单任务 4） |

### ✅ 待验证项已全部确认（2026-09 复核）

| 项 | 问题 | 结论 |
|---|---|---|
| M1 | npm test flaky / skip pattern | **已解决**：package.json:35-36 已改两阶段结构（unit skip integration && integration 单独跑）；全量测试已实测通过（35 集成全过 exit 0） |
| M6 | npm audit 3 漏洞 | **已处理**（fast-uri/xmldom/qs，已实测跑过） |
| M7 | 数据无上限增长 | **部分已修**：`edge-session.mjs:55-62` 复制前清理 Cache_Data/GPUCache 等缓存残留（注释"性能工单 M7"）；日志轮转/tool_results 清理未确认 |
| L5 | lint 1 error | **已修复**：`project-eval/skill.mjs:65` 已改声明后赋值（无 useless-assignment） |
| S9 | Edge 全量 cookie 复制 | **已修复**：`edge-session.mjs:105-113` 只保留牛客域 cookie 行 + VACUUM 缩体积（注释"安全工单 S9"） |

## 执行顺序建议（按验收后剩余项）

```
阶段一（面试前）：
  ① 测试与CI工单：补沙箱回归测试（S2/S3 修复后需测试覆盖）+ M8 集成测试临时目录
  ② 安全工单剩余：L6（安全模块补测试）+ L10（oj.mjs 复用 assertPublicUrl）
  ③ 发布与文档工单：M13（mcp.md 9→13）+ L11 硬编码路径
  ④ TS 升级工单任务 1：JSDoc + checkJs（0.5 天）

阶段二（面试前有余力）：
  ⑤ 性能工单：M9 轮询门控 + M10 AudioWorklet（React 版语音可能录不到声音）

阶段三（面试后）：
  ⑥ 技术债清理工单（L1 抽 json-utils 等）/ 性能工单任务 4（cc-watcher/rag/memory）/ TS 升级任务 2-3
```

## 面试叙事素材（做完后可用）

```
"基于 8 轮审计的评估报告分发 7 个工单，验收时发现报告已过期——
9 个严重项 8 个已修复（沙箱逃逸/断言伪造/IPC 桥泄露/3 处静默吞错），
按剩余项重新排期：先补沙箱回归测试，再修轮询与麦克风，最后做渐进式 TS 升级。"
——体现：评估→分发→验收→再排期的闭环工程习惯
```
