# 技术债清理工单（评估报告 L1-L4/L7/L9/L12/L13/L15）

> 背景：项目全面评估报告-2026-09 技术债维度 7.5/10——几乎没有 TODO/FIXME 堆积、模块拆分彻底是亮点，但重复实现多（extractJson ×5、日期 ×20 处含时区混用、smartSlice/withTimeout/clean/文件名安全化 ×2-3、project-eval 与 interview-prep 常量逐字节相同）、硬编码残留、废弃残留。**本工单为低优先，面试前可只做 L1/L2 抽公共工具（也是面试叙事素材）。**

---

## 一、问题清单

| # | 问题 | 位置 |
|---|---|---|
| L1 | `extractJson` 复制 5 处（mail/rss 版支持数组形态） | `ai.mjs:8`/`dreaming.mjs:13`/`mail.mjs:298`/`rss.mjs:85` |
| L2 | 日期工具复制（parseLocalDate ×2、格式化 ~20 处，UTC/本地混用） | `job-match.mjs:31`/`job-reminders.mjs:6` 等 |
| L3 | smartSlice/withTimeout/clean()/文件名安全化各 2-3 处重复 | `study-plan.mjs:318`/`study-review.mjs:36`/`mail.mjs:72`/`rss.mjs:35`/`jobs.mjs:264`/`job-collect.mjs:15`/`zhenti.mjs:210`/`study-files.mjs:16`/`output-import.mjs:9`/`patrol.mjs:220` |
| L4 | 端口 8899 硬编码 5 处，未尊重 `MIANSHI_PORT` | `api-client.mjs:4`/`panel-core.js:9`/`core.mjs:36`/CSP×2/`lib/widget-auth.mjs:12` |
| L7 | `submitAnswer` 208 行 + 内嵌 70 行 prompt + 魔法数字 | `interview-session.mjs:12-220` |
| L9 | `data/*.bak` 4 个残留 + `_panelOpen` 死代码（写入从未读取） | `data/`、`main.mjs:1238-1242` |
| L12 | eval-summary CSV 无转义（含逗号字段破坏 CSV） | `lib/eval-summary.mjs:18-19` |
| L13 | ASR worker 无退出清理（before-quit 未 terminate） | `desktop/main.mjs:758-783` |
| L15 | project-eval 与 interview-prep 常量逐字节相同 | `skills/project-eval/skill.mjs:14-19` vs `skills/interview-prep/skill.mjs:114-119` |

## 二、任务

### 任务 1：抽公共工具（0.5-1 天，优先做）

```
① 抽 lib/json-utils.mjs：extractJson 统一（含数组形态，mail/rss 版行为合并）
② 抽 lib/date-utils.mjs：parseLocalDate + localDateKey + 格式化（统一本地时区口径，
   配合数据完整性工单 M12）
③ 抽 lib/string-utils.mjs：smartSlice / clean() / 文件名安全化
④ 抽 lib/async-utils.mjs：withTimeout
⑤ 各调用点替换，删原实现
```

### 任务 2：硬编码与死代码（0.5 天）

```
① 端口 8899 收编单一来源（lib/config 或 env），5 处引用统一读取（尊重 MIANSHI_PORT）
② 删 data/*.bak 4 个 + main.mjs:1238-1242 _panelOpen 死代码
③ eval-summary.mjs CSV 加引号转义
④ main.mjs:758-783 ASR worker before-quit terminate
```

### 任务 3：大函数与常量（0.5 天，可选）

```
① interview-session.mjs submitAnswer 拆分：70 行 prompt 抽常量 + 判分逻辑抽函数
② project-eval 与 interview-prep 共享常量抽到 skills/_shared/（或各自 import 公共常量文件）
```

## 三、验收

```
① grep 确认 extractJson/parseLocalDate/smartSlice/withTimeout 全库仅 1 处实现
② 全部 35 个集成测试 + 相关单元测试通过（替换无回归）
③ 改 MIANSHI_PORT 环境变量后 5 处端口引用全部生效
④ git status 无 data/*.bak
⑤ lint 0 error
```
