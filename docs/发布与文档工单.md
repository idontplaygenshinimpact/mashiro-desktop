# 发布与文档工单（评估报告 S8 + M13 + L5/L8/L11/L14/L16）

> 背景：项目全面评估报告-2026-09 可维护性维度 8/10——文档体系惊人（docs/ 45 文件），但**发布包 files 缺技能导致 MCP 两个工具装包即坏**（严重），且文档数字普遍过期、命名语义重叠、脚本硬编码绝对路径。

---

## 一、问题清单

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| S8 | 发布包 `files` 缺 `skills/dev-history-guide/`（只含 project-guide，但 `mcp-server.mjs:257` import 它） | `package.json` files vs `mcp-server.mjs:257` | 发布后 read_dev_history / generate_dev_history_guide 两个 MCP 工具运行失败 |
| M13 | mcp.md 声称 9 工具，实际注册 13 个（漏 generate_project_guide/read_project_file/read_dev_history/generate_dev_history_guide） | `docs/mcp.md:85` vs `mcp-server.mjs` | 文档过期误导用户 |
| L5 | README 声称 924 用例/90 文件（实测 106 文件 1021 用例）；plugin-architecture.md "730+ 测试" 与 ":132,138,171" "639 测试" 自相矛盾；phase-p1-skills "现有 6 个技能"（实际 10）；phase-eval "questions 14"（实际 38） | `README.md:295`、`docs/plugin-architecture.md`、`docs/phase-p1-skills.md:3`、`docs/phase-eval.md:13` | 文档失真 |
| L8 | 命名语义重叠 study/learning/learning-plan；skills 命名空间连字符/下划线混用（`skill__company-intel__` vs `skill__project_eval__`） | `lib/`、`skills/` | 误用风险 |
| L11 | 正式脚本硬编码绝对路径 `D:/mianshi-agent/` | `verify-final-menu.mjs:4-5`、`verify-interaction.mjs:4-5`、`web-task-bench.mjs:270`、`import-ai-career.mjs:8` | 换机器即失效 |
| L14 | 硬编码"448 道"题库数 | `lib/interview-agent.mjs:14` | 题库变化后描述失真 |
| L16 | 评测扩容目标未达成（目标 60+/40+/30+，实际 38/16/12） | `docs/phase-eval-方案.md:57-62` | 路线图欠账 |

## 二、任务

### 任务 1：修发布包（0.5 天，严重）

```
① package.json files 补 "skills/dev-history-guide/"
② 验证：npm pack 后解包检查 skills/ 完整 + mcp-server import 链可解析
③ 顺手核对 files 是否覆盖全部 10 个技能（project-guide/dev-history-guide/...）
```

### 任务 2：文档数字校准（0.5 天）

```
① README 用例数/文件数改为实测值（106 文件 1021 用例）或标注"实测日期 2026-09-04"
② plugin-architecture.md 统一测试数（730 vs 639 矛盾处）
③ phase-p1-skills 技能数 6→10；phase-eval questions 14→38
④ mcp.md 工具清单 9→13（补 generate_project_guide/read_project_file/read_dev_history/generate_dev_history_guide）
```

### 任务 3：命名与路径清理（0.5 天）

```
① skills 命名空间统一：下划线改连字符（skill__project_eval__ → skill__project-eval__）
   ——注意同步 skills.mjs 发现逻辑与 README 约定
② 正式脚本绝对路径改相对路径/参数化（verify-final-menu/verify-interaction/web-task-bench/import-ai-career）
③ interview-agent.mjs:14 "448 道"改动态注入（读题库表 count）
④ study/learning/learning-plan 三模块加职责注释（不重命名，避免大改）
```

## 三、验收

```
① npm pack 解包：skills/dev-history-guide/ 存在，MCP 13 工具全部可加载
② README/plugin-architecture/mcp.md 数字与实测一致（或标注日期）
③ skills 命名空间全库统一连字符
④ 脚本在非 D:/mianshi-agent 路径可运行
⑤ 题库数动态获取，无硬编码
```
