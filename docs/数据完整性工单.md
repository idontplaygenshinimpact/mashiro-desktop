# 数据完整性工单（评估报告 S5-S7 + M11/M12）

> 背景：项目全面评估报告-2026-09 代码质量维度发现 **3 处静默吞错的数据完整性 bug**（用户答案/复习卡/学习清单被静默丢失且无感知）+ 2 处事务/时区问题。对比 `review.mjs:170-181` 复习写库用 withTx 且失败透传——同模块两种标准并存，需统一为"失败必须可见"。

---

## 一、问题清单

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| S5 | 复盘判分 `extractJson` 解析失败时 `results=[]`，但**无条件标记 reviewed=true 并 savePlan** | `lib/study-review.mjs:87-98` | 用户答案被静默消费，复盘进度被消耗、错题不回流，返回 `{ok:true,results:[]}` 无错误信号 |
| S6 | `addCard` 的 INSERT 包在 `catch { /* ignore */ }`，但函数末尾 `return card` 返回内存卡对象 | `lib/review.mjs:123-127` | 调用方（study-plan.mjs:291 / ai-career.mjs:149 / interview-scoring.mjs:82）以为建卡成功，重启即消失 |
| S7 | `savePlan` 整库重写（DELETE+INSERT）包在 `catch { /* ignore */ }` | `lib/study-store.mjs:63` | 学习清单唯一持久化入口，失败无任何信号 |
| M11 | `indexPersonalProjects` 删除+插入不在事务 | `lib/personal-projects.mjs:129-134` | 崩溃丢整份项目档案 |
| M12 | UTC/本地时区混用（10+ 处 `toISOString().slice(0,10)`） | `learning-plan.mjs:196`（UTC）vs `job-reminders.mjs:53`（本地） | 东八区 0-8 点跨天分桶错位 |

## 二、任务

### 任务 1：修 3 处静默吞错（0.5 天）

```
① study-review.mjs:87-98：extractJson 解析失败 → 返回 {ok:false, error} 且不标记 reviewed
   （错题必须回流，复盘进度不被消耗）
② review.mjs:123-127：addCard 写库失败 → 返回 {ok:false} 或 console.warn + 不返回成功卡
   （对齐同文件 170-181 的 withTx 透传标准）
③ study-store.mjs:63：savePlan 失败 → console.error + 返回错误（至少可观测）
④ 三处调用方（study-plan.mjs:291 / ai-career.mjs:149 / interview-scoring.mjs:82）处理错误返回
```

### 任务 2：事务与时区（0.5 天）

```
① personal-projects.mjs:129-134：indexPersonalProjects 包 withTx（删除+插入原子化）
② 抽 lib/date-utils.mjs 的 localDateKey()（本地时区口径）：
   替换 learning-plan.mjs:196 等 UTC 用法，统一全库日期分桶
```

## 三、验收

```
① 判分失败场景：返回 {ok:false}、reviewed 不变、错题仍在待复盘队列（mock LLM 乱码测试）
② addCard 写库失败：调用方收到失败信号（mock DB 故障测试）
③ savePlan 失败：日志有 console.error（可观测）
④ indexPersonalProjects 崩溃中断：不产生半份档案（事务回滚）
⑤ 东八区 0-8 点：日期分桶与本地日历一致（跨天测试）
⑥ 全部 35 个集成测试通过
```
