# 收尾工单（7 工单验收后的剩余项）

> 背景：2026-09 对 7 个工单（安全/数据完整性/测试CI/发布文档/性能/技术债/TS升级）逐项验收，**6 个已全部完成**（评估报告 09-04 之后任务已执行，代码注释含工单编号标记）。仅剩 3 个收尾项：TS 升级 JSDoc 缺口 ×2 + memory 全表扫描（低优先）。
> **2026-09 验收：3 项全部完成 ✅**（任务 1/3 已存在，任务 2 本次补齐 27 个函数 JSDoc；typecheck EXIT=0，unit 998 pass 0 fail）

---

## 一、剩余项清单

| # | 问题 | 位置 | 优先级 |
|---|---|---|---|
| ① | `lib/memory.mjs` 0 条 JSDoc（工单点名要标的模块完全没标） | `lib/memory.mjs` | 中 |
| ② | `lib/tools/impl-*.mjs` 基本无 JSDoc（impl-search/impl-interview/impl-fetch 等核心工具，仅 impl-study 3 条 + exec-tools 1 条） | `lib/tools/` | 中 |
| ③ | `mergeSimilarWeakPoints` 全表 O(N²)（每 12h 一次） | `lib/memory.mjs:436-439` | 低（可不做） |

## 二、任务

### 任务 1：补 memory.mjs JSDoc（0.5 天）

```
① 为 lib/memory.mjs 全部导出函数补 @param/@returns：
   - 相似度判断（isSimilarWeakTopic / isSimilarTopicForArchive——含 GENERIC_3GRAM 排除逻辑）
   - addWeakPoint / mergeSimilarWeakPoints / 内存镜像加载
② 关键数据结构补 @typedef（如 WeakPoint 形状：topic/fail_count/last_failed_at/source/origin）
③ 验收：npm run typecheck 仍全绿（EXIT=0）
```

### 任务 2：补 tools/impl-*.mjs JSDoc（0.5 天）

```
① lib/tools/ 下 impl-search.mjs / impl-interview.mjs / impl-fetch.mjs / impl-study.mjs 等
   全部工具函数补 @param/@returns（工具参数形状 + 返回形状）
② 与 lib/llm.mjs 的 @typedef（LLMResponse 等）复用，不重复定义
③ 验收：npm run typecheck 仍全绿
```

### 任务 3：memory mergeSimilarWeakPoints 优化（可选，0.5 天）

```
① mergeSimilarWeakPoints 全表 O(N²) → 按 fail_count 排序只取 Top N 参与合并
   （或加 topic 前缀索引/抽样——低频任务，收益有限）
② 不做也行——每 12h 一次，数据量小（weak_points 通常 <200 行）
```

## 三、验收

```
① lib/memory.mjs 与 lib/tools/ 全部导出函数有 JSDoc（grep @param/@returns 覆盖）
② npm run typecheck EXIT=0
③ npm test 全绿（无回归）
④ （可选）mergeSimilarWeakPoints 有性能注释或优化
```

## 四、面试叙事素材

```
"7 个工单验收后只剩 TS 升级的 JSDoc 缺口：
memory.mjs 和 tools/ 核心工具补类型标注，让 checkJs 真正覆盖全 lib/——
类型检查从'只查已标注部分'变成'全量覆盖'。"
```
