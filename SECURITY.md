# 安全政策

## 报告漏洞

发现安全问题时，**不要**公开创建 Issue（避免在修复前暴露细节）。请通过以下方式报告：

- GitHub 私信仓库维护者
- 或先在 Issue 中用模糊描述确认维护者联系方式

## 本项目关注的安全边界

1. **数据安全**：用户数据（简历/清单/复习卡/岗位/讲解存档）只在本机存储与处理；任何改动不得引入数据丢失风险。数据层有自动备份（`lib/backup.mjs`，每天一次 + 恢复前快照）。
2. **SSRF**：所有对外抓取（爬取/浏览工具）必须经过 `lib/fetch-page.mjs` 的统一校验（内网 IP/环回/云元数据/域名重绑定拒绝）。绕过该 choke point 的新入口属于高危。
3. **提示注入**：外部内容（爬取页面/知识库/网页工具结果）进入 LLM 上下文必须经 `sanitizeExternal()` 包裹为不可信数据；面板/agent 侧不得信任外部指令。
4. **本地服务鉴权**：`widget` 服务的 `/api/*`（除 `/api/health`）要求 Bearer token（`data/widget-token.json`）；CORS 白名单只放行本机页面。新增路由不得绕过鉴权。
5. **插件安全**：插件 id 白名单（`^[a-z0-9-]+$`）、设置命名空间 `plg_<id>_` 隔离、市场安装做路径穿越校验与 manifest 一致性校验。

## 漏洞处理流程

1. 确认漏洞与影响面（数据泄露 / 远程执行 / 数据丢失 / 越权）
2. 修复 + 回归测试（复现用例先落测试）
3. 发布修复版本并同步说明

## 版本支持

| 版本 | 安全修复支持 |
|---|---|
| main 分支 | ✅ 持续维护 |
| 已发布 release | 按需修复重大漏洞 |
